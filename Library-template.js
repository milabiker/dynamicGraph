

GraphBackground = function(){
	_wrapper; //svg element
	_options={}

	function setGridlines(xAxisValues,yAxisValues, type);
	function setBgColor(color);
	yAxis = graphAxis(grapharea, 'y', title, min, max, ticks);
	xAxis = graphAxis(grapharea, 'x', title, min, max, ticks);
}

GraphAxis = function(grapharea, title, min, max, ticks, values_type){
	_grapharea = grapharea
	_title = title
	_values[];
	_values_type;

	drawAxis(grapharea);
	redraw()?
	rescale;
	calculate	
}
GraphLegend = function(svg, {'seriesName'}, settings ){


}
GraphSeries = function(area, seriesValues, setting) {
	_values;
	addSeries();
	addValue();

}

function AjaxCallback(){


}